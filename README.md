# Segmentaion-via-GMM
We present an automated pipeline for medical image segmentation that utilizes unsupervised pseudo-labels to train a supervised deep learning model (U-Net). We investigate this approach across two distinct clinical tasks: brain MRI skull-stripping on the SynthStrip 2D dataset, and zero-shot cross-modality liver segmentation on the CHAOS dataset. 
